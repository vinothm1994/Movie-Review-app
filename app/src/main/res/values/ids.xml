<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- These ids are used in both the menu (bottom navigation.xml) and the nav graph (mobile_navigation.xml).
        Each menu item and nav graph fragment represent the same logical item. -->
    <item name="navigation_home" type="id" />
    <item name="navigation_notifications" type="id" />
    <item name="navigation_favorite" type="id" />
    <item name="navigation_message" type="id" />
    <item name="navigation_profile" type="id" />

</resources>